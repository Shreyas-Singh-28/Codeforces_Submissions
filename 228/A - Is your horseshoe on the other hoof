#include <bits/stdc++.h>
 
using namespace std;
using vi = vector<int>;
 
void solve() {
    int s1,s2,s3,s4; cin>>s1>>s2>>s3>>s4;
    set<int> s = {s1,s2,s3,s4};
    cout<<4-s.size();
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    solve();
}